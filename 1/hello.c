#include <stdio.h>			// include information about standard library

int main() {				// define a function called main that takes no arguments
	printf("hello, world\n");	// call printf function defined in the standard library with a
	return 0;			// sequence of characters as an argument, ended with a newline
}
